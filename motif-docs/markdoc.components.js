import { Button } from '@components/button'
import { Collapse } from '@components/collapse'
import { Image } from '@components/image'
import { Note } from '@components/note'
import { Hero, HeroExtraLinks, HeroLinkCards } from '@components/hero'
import {
  Card as RoadmapCard,
  Heading as RoadmapHeading,
} from '@components/roadmap'

import { Spacer } from '@components/spacer'
import { Div } from '@components/div'

import { SampleBadge } from '@components/badge'
import { SampleGuidesGallery } from '@components/guides-gallery'
import { SampleCodeGallery } from '@components/code-gallery'
import { SampleChangelog, ChangelogEntry } from '@components/changelog'
import { DiscordIcon } from '@components/icons'
export default {
  Note,
  Collapse,
  Button,
  Hero,
  HeroExtraLinks,
  HeroLinkCards,
  Image,
  RoadmapCard,
  RoadmapHeading,
  Div,
  Spacer,
  SampleGuidesGallery,
  SampleCodeGallery,
  SampleChangelog,
  SampleBadge,
  ChangelogEntry,
  DiscordIcon
}
