import { Button } from "@components/button"
import { BookIcon, DiscordIcon, DoneIcon, PaperIcon, SupportIcon, TwitterIcon } from "@components/icons.mdx"

export const Hero = () => {
  return <div className="hero-gradient rounded-lg px-12 py-16 not-prose antialiased">
    <h1 className="text-4xl text-white font-bold">Flightcontrol Documentation</h1>
    
    <h2 className="text-xl text-white mt-4 mb-12">Deploy, preview & scale your apps with AWS easily</h2>
    <Button large title="Get started →" href="/docs/guides/get-started" />
  </div>
}

<Hero />

export const HeroLinkCard = ({ title, subtitle, Icon, href }) => {
  return <a href={href} className="unstyled-link flex flex-row items-center gap-4 p-4 rounded-md hover:bg-gray-50 cursor-pointer transition not-prose">
      <div className="w-16 h-16 border border-fuchsia-100 p-3 text-fuchsia-700 rounded-md">
        <Icon />
      </div>
      <div className="flex flex-col col-span-3 gap-1 flex-grow">
        <div className="text-lg font-medium not-prose">{ title }</div>
        <div className="text-gray-500 no-underline">{ subtitle }</div>
      </div>
    </a>
}

export const HeroLinkCards = () => {
  return <div className="grid grid-cols-1 sm:grid-cols-2 gap-4 prose max-w-full w-full">
    <HeroLinkCard
      href="/docs/guides/get-started/"
      title="Getting Started"
      subtitle="Learn the basics to get up and running."
      Icon={BookIcon}
    />
    <HeroLinkCard
      href="/docs/http-api"
      title="API Reference"
      subtitle="Check the available methods"
      Icon={PaperIcon}
    />
    
    <HeroLinkCard
      href="/docs/troubleshooting"
      title="Troubleshooting"
      subtitle="Get help fixing common problems."
      Icon={SupportIcon}
    />
    <HeroLinkCard
      href="/docs/changelog"
      title="Changelog"
      subtitle="See what we've shipped"
      Icon={DoneIcon}
    />
  </div>
}

<HeroLinkCards />

export const HeroExtraLink = ({ title, Icon, iconClassName, href}) => {
  return <a href={href} className="unstyled-link flex flex-row gap-4 items-center px-4 py-2 hover:opacity-80 transition">
      {Icon && <Icon className={`w-6 h-6 ${iconClassName}`} />}
      <div className="flex-grow">{title} →</div>
    </a>
}

export const HeroExtraLinks = () => {
  return <div className="grid grid-cols-1 sm:grid-cols-2">
    <div>
      <HeroExtraLink
          Icon={DiscordIcon}
          title="Join us on Discord"
          href="https://discord.gg/yY8rSPrD6q"
          iconClassName="text-[#5A64EA]"
        />
      <HeroExtraLink
          Icon={TwitterIcon}
          title="Follow us on Twitter"
          href="https://twitter.com/flightcontrolhq"
          iconClassName="text-[#1DA1F2]"
        />
      </div>
      <div>
        <HeroExtraLink
            title="Flightcontrol Dashboard"
            href="http://app.flightcontrol.dev/"
          />
      </div>
    </div>
}

<HeroExtraLinks />